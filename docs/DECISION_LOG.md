# Decision Log — RGCS v3 / RSCS 1.0 programme

Append-only. Format: ID, date, decision, rationale, alternatives, scope.

| ID | Date | Decision | Rationale / alternatives | Scope |
|---|---|---|---|---|
| D3-001 | 2026-07-14 | **Phase 0 naming freeze:** math framework "RSCS 1.0"; software/system "RGCS 3.0.0"; repository remains this git repo (branch `main`), package name `rgcs-v3`. New-code namespaces: `rscs_core` (framework), `RSCS-O.*`/`RSCS-C.*` registry IDs. | Pack default names accepted; a separate `rgcs-v3` repo was the alternative, but the v2 tree is already here and git tag `v2.0.0` + `archive/v2.0.0/` satisfy the baseline-freeze requirement without splitting history. | All code, docs, manuscripts, schemas, citations, release artifacts. Frozen hereafter. |
| D3-002 | 2026-07-14 | v2.0.0 baseline frozen as commit `f9fd2c2` / tag `v2.0.0`; v2 release artifacts moved (not copied) to `archive/v2.0.0/release/`. | Single source of truth; avoids the duplicate-tree condition Agent 01 must prevent. | archive/v2.0.0 immutable. |
| D3-003 | 2026-07-14 | v2 test suite discrepancies on Windows/Py3.13 (4 of 227) classified as environment drift + v2 Windows portability defects (V2-WIN-01, V2-PKG-01); v2 code NOT patched. Fixes scheduled for v3 (MIG-CODE-07). | Freeze rule outweighs green-suite aesthetics; defects documented in V2_BASELINE_AUDIT.md. | rgcs_desktop, packaging. |
| D3-004 | 2026-07-14 | `pyproject.toml` updated to `rgcs-v3` 3.0.0a1 with `pyyaml` declared (fixes V2-PKG-01 for v3 builds); `.gitattributes` added (`* text=auto`) to stop LF/CRLF churn. | v2 pyproject preserved in tag; declaring a dependency the code already imports is a packaging correction, not a behaviour change. | Build/packaging only. |
| D3-005 | 2026-07-14 | v3 skeleton created per `output_contracts/EXPECTED_TREE.md`: `rscs_core/` (10 subpackages), `manuscripts/` (4), `embedded/`, `references/`, `tests/adversarial/`, `release/` (empty, v3-reserved), CHANGELOG.md. Deviations: v2 `manuscript/` retained until Agent 09 migrates it (then archived); `docs/` continues in place rather than being recreated. | Deviations recorded here and to be echoed in ARCHITECTURE.md / RELEASE_CHECKLIST.md per contract. | Repo layout. |
| D3-006 | 2026-07-14 | **RSCS namespaces frozen:** `RSCS-C.*` = coordinate/schema IDs, `RSCS-O.*` = operator IDs; `RSCS-M.*` reserved and unused (avoids confusion with frozen `RGCS-M.1…61`). Symbol layer in `docs/RSCS_NOTATION_LEDGER.md`. | Keeps v2 equation IDs immutable while giving RSCS its own append-only space; alternative (renumbering into RGCS-M) rejected as it would touch frozen IDs. | All Agents 03–11. |
| D3-007 | 2026-07-14 | **Coupling sign convention frozen for all RSCS operators:** `𝒦` uses anti-Hermitian `K = i·2πg` (inherits QA-D-04). Any deviating coupling requires a DECISION_LOG entry + INCONSISTENCY row. | One convention across v2 and v3 prevents the exact class of error QA-D-04 fixed. | rscs_core.coupling, Agents 03/06. |
| D3-008 | 2026-07-14 | **Agent 02 gate declared OPEN.** Frozen notation ledger + source registry + equation-provenance ledger + adaptation/exclusion matrices are complete; Agents 03–08 may proceed (in parallel). | Orchestrator requires frozen notation, source ledger, and migration map before 03–08; all now exist. | Programme sequencing. |
| D3-009 | 2026-07-14 | **RSCS state representation = typed product/fibre state, not a flat vector.** Coordinates are immutable frozen dataclasses on declared manifolds (S¹/ℤ/SO(3)/ℂⁿ/…), validated at construction. | Quality Gate 5 forbids flat-vector ambiguity; a bag of floats loses topology and units. Alternative (numpy record arrays) rejected as it does not carry per-field manifolds or reject NaN at construction. | rscs_core.coordinates; Agents 04–08 APIs. |
| D3-010 | 2026-07-14 | **rscs_core layout:** operators live in the EXPECTED_TREE subpackages (transforms/coupling/modes/propagation/state_preparation/observation/uncertainty/provenance/memory); an additive `operators/` package re-exports all 13 flat, and `embedding/`, `registry/`, `units.py` are added. No EXPECTED_TREE dir removed. | Satisfies both the output contract tree and the Agent 03 brief's `operators/` concept without a second home for any operator. Additive deviation recorded here per EXPECTED_TREE §note. | Repo layout. |
| D3-011 | 2026-07-14 | **Conservative-extension tolerances:** rtol 1e-9, atol 1e-12 (`rscs_core/units.py`). New v3 numeric tests are tolerance-aware (MIG-TEST-02); frozen v2 byte-equality tests are untouched. | Tight enough to catch structural error, loose enough for last-ulp float drift (NR3-001). | rscs_core.embedding; all CEP tests. |
| D3-012 | 2026-07-14 | **Claim firewall rule = "no strong-from-weak".** A DER/EST (rank≥3) output is rejected when any input is HYP/ENG/SRC (rank<3); the weak→weak boundary move SRC/HYP→HYP/ENG is allowed. | Matches design principle 4 exactly ("no SRC into EST/DER without a HYP/ENG boundary"). Initial "output ≤ weakest input" draft wrongly blocked the SRC→HYP boundary and was corrected after the test caught it. | rscs_core.registry, rscs_core.provenance. |
| D3-013 | 2026-07-14 | **Error-handling policy:** invalid coordinates/operators raise at construction/call (ValueError/TypeError); NaN/inf never propagate; JSON uses null-not-NaN (v2 rule); the quarantined C.14/O.13 require an explicit `acknowledge_hypothesis` flag. | Fail loud and early; keep the HYP layer un-enterable by accident. | rscs_core. |
| D3-014 | 2026-07-14 | **RSCS Foundations manuscript deferred to Agent 09** (only the model/registry/schema docs authored now). | Agent 03 owns the mathematical backbone + stable APIs; manuscript production is Agent 09's dependency boundary. Draft skeleton left under `manuscripts/rscs_foundations/`. | Programme sequencing. |
| D3-015 | 2026-07-14 | **`docs/model_registry.yaml` NOT bumped to schema 2; RSCS uses its own machine registry** `rscs_core/registry/rscs_registry.yaml` (schema 1, 27 entries). The frozen RGCS registry stays schema 1 / 61 entries / untouched. | The migration map floated "schema 2 adds RSCS namespace", but editing the frozen registry would change a file the v2 app reads (`MODEL_VERSION` = "registry schema 1") and break the immutability guarantee; a separate namespaced RSCS registry (D3-006) achieves the same traceability with zero risk to the baseline. The adversarial test `test_rgcs_registry_still_61_and_schema1` pins this. | Registry. |
| D4-001 | 2026-07-14 | **Notation ledger append (governance path):** RSCS-C.15 (Hydrogenuine memory record, ENG) + RSCS-O.14/15/16 (HG store/replay/update, ENG). Added to `docs/RSCS_NOTATION_LEDGER.md` §4a before first use. | The §4 governance path requires an append + this log entry before a new symbol is used. The HG record is an engineering data structure; keeping it ENG (not HYP) lets it serve as ordinary spatial memory while the NHT encoding (O.3) and HAL lattice (O.13/C.14) stay HYP-quarantined. | Agent 04; rscs_core.memory. |
| D4-002 | 2026-07-14 | **HG memory record classified ENG; NHT space-to-phase + HAL lattice stay HYP.** The record structure and its store/replay/update are engineering abstractions (never evidence); only the NHT phase encoding and HAL lattice carry the HYP class and the acknowledge-hypothesis quarantine. | Separates a useful, testable software artifact from the unproven neuroscience claim (design principle 4 firewall). Alternative (make the whole record HYP) rejected: it would forbid using spatial memory without endorsing NHT. | Agent 04. |
| D4-003 | 2026-07-14 | **Falsifiable software claims H-15..H-19 registered** (retrieval quality, transform consistency, temporal continuity, localization/replay fidelity, uncertainty calibration) in `docs/NHT_HAL_RSCS_MAPPING.md` + `docs/CLAIM_REGISTER.md`, each with a pre-registered failure condition. This satisfies the Agent 03 handoff requirement to attach an observable + failure condition before using the HYP memory layer. | Quality Gate 7: every HYP needs an observable, controls, uncertainty, success threshold, and failure condition. v3 hypotheses append H-15+ (migration rule). | Agent 04; QA. |
