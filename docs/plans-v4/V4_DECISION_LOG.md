# V4 Decision Log — RGCS v4 / RSCS 2.0

Append-only. Planning-pass decisions. Key rows are mirrored into the
top-level `docs/DECISION_LOG.md`. Format: ID, date, decision, rationale,
scope. This log governs the v4 planning package only; production
implementation is out of scope for this pass.

| ID | Date | Decision | Rationale / alternatives | Scope |
|---|---|---|---|---|
| DV4-001 | 2026-07-16 | **v4 names:** software/system "RGCS 4.0.0"; mathematical layer "RSCS 2.0". v2.0.0, v3.0.0, `archive/v2.0.0/`, frozen `RGCS-M.*`, and frozen `RSCS-C.*`/`RSCS-O.*` are immutable historical baselines. | Continues the freeze-and-conservatively-extend discipline (v3→v2). No existing tag, equation, symbol, class, or source interpretation is rewritten. | All v4 planning + future implementation. |
| DV4-002 | 2026-07-16 | **New RSCS 2.0 namespaces (zero collision with frozen ids):** `RSCS2-E.*` governing/constitutive equations; `RSCS2-G.*` geometry & mesh; `RSCS2-B.*` boundary operators; `RSCS2-S.*` solver operators; `RSCS2-A.*` accelerator operators; `RSCS2-D.*` field/eye diagnostics; `RSCS2-V.*` validation benchmarks; `RSCS2-U.*` uncertainty & inverse-model operators. | The `RSCS2-` prefix guarantees no overlap with `RGCS-M.*`, `RSCS-C.*`, `RSCS-O.*`, or `RSCS-M.*` (reserved-unused). Each new object needs id+signature+units+class+provenance+exclusions+module+tests+doc-target before first use (governance path). | Registry, all v4 specs. |
| DV4-003 | 2026-07-16 | **Planning docs live in tracked `docs/plans-v4/`**, not gitignored `internal-docs/`. | Another autonomous run must be able to execute them and update living registers; public tracking matches how all other planning docs (`SPEC.md`, `TEST_PLAN.md`, …) live under `docs/`. | Repo layout. |
| DV4-004 | 2026-07-16 | **CPU backend is the numerical authority, forever.** Every accelerated/approximate path ships with a CPU reference and a parity test; the project is fully usable with **numpy+scipy only**. | Inherits the MODELLING_ROADMAP §0 reference-oracle rule. Local audit: only numpy/scipy/matplotlib/pydantic/yaml present, no GPU — so CPU-first is also the only honestly testable path here. | All solver/accelerator work. |
| DV4-005 | 2026-07-16 | **Provisional CPU stack (smallest reliable, permissive-licensed):** numpy, scipy (present); **scikit-fem** (BSD-3, pure-Python FEM assembly, numpy/scipy-only) as the reference FEM layer; **meshio** (MIT) for mesh format conversion; **matplotlib** (present) as the always-available 2D/report renderer; JSON+`.npz` as the always-available serialization. Heavy/optional: Gmsh, PETSc/SLEPc, pyvista/VTK, h5py/XDMF, trimesh. | scikit-fem needs nothing beyond scipy → keeps CPU-only install trivial. Everything heavier is optional behind capability detection. | Dependency policy, all tranches. |
| DV4-006 | 2026-07-16 | **Gmsh is an EXTERNAL TOOL, not a linked dependency.** Meshing via Gmsh is invoked as a subprocess / file interchange (`.geo`/`.msh` ↔ meshio), never imported into the MIT codebase. | Gmsh is GPL; subprocess/file interchange keeps the MIT licence clean while still supporting Gmsh users. meshio (MIT) does the format bridge. | Geometry/mesh spec; licensing. |
| DV4-007 | 2026-07-16 | **GPU acceleration ships behind a four-status ladder** INTERFACE_TESTED → NUMERICALLY_PARITY_TESTED → HARDWARE_BENCHMARKED → MULTI_DEVICE_REPRODUCED. **No hardware-benchmark claim without compatible hardware.** CuPy is the provisional primary GPU backend (numpy-drop-in → best parity story); PyOpenCL secondary. Both optional. | Local machine has no NVIDIA GPU/driver; this pass can plan only to INTERFACE/PARITY. Prevents "it imports, therefore it's fast" dishonesty. | Accelerator spec, acceptance. |
| DV4-008 | 2026-07-16 | **The crystal remains the primary example; two conventional reference systems (tuning fork; EB/Timoshenko cantilever) are MANDATORY** validation gates before any crystal "eye" result is reported. | Prevents crystal-specific bugs from masquerading as physics; conventional systems have closed-form/published answers. | Reference-systems spec, acceptance. |
| DV4-009 | 2026-07-16 | **Conservative Extension for v4:** every v4 solver must reproduce the frozen v3 numbers on the v3 domain — specifically α-quartz axis wave speeds (frozen `RSCS-O.17`/`rgcs_core.anisotropy`) as benchmark RSCS2-V.6, and the degenerate two-mode splitting (frozen `RSCS-O.4`/`RGCS-M.24`) as RSCS2-V.9 — within declared tolerance, machine-tested in CI. | "Same trick, one generation up." v4 provably cannot silently change v3. | Solver/quartz/benchmark specs. |
| DV4-010 | 2026-07-16 | **The historical "eye" stays SRC. Any computed eye candidate is DER (a field diagnostic) or HYP (a falsifiable claim), never EST.** An eye result requires the full robustness battery (mesh persistence, uncertainty persistence, cross-channel agreement, boundary-perturbation stability) or it is reported as NULL. | Brief boundary; extends D6-003/node-menu discipline. No unique eye coordinate without robustness + uncertainty. | Eye-diagnostics spec, claims H-31+. |
| DV4-011 | 2026-07-16 | **v4 claims start at H-31** (append-only after the frozen H-01..H-30). Falsifiable v4 hypotheses (eye robustness, cross-channel agreement, inverse-fit generalization) register there with observable/controls/failure-condition. | Continues the claim register without touching frozen rows. | Claim register. |
| DV4-012 | 2026-07-16 | **No new physics in this pass.** All `RSCS2-E.*` governing equations are standard continuum mechanics / piezoelectricity / optics (EST) with textbook provenance; the project *composes and validates* them, it does not derive new ones. Anything a future measurement demands goes through the ledger governance path first. | Brief rule "publish no new physics"; keeps every equation classifiable EST with a citation. | Math-model plan. |
