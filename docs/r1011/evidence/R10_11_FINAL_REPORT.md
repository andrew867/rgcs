# R10.11 Final Report — Unified Codec + Flat-Face Node-Curvature Map (2026-07-27)

**Verdict:
`RGCS_R10_11_YELLOW_FLAT_FACE_MAP_RECOVERED_CODEC_ALIASES_REMAIN`**

Precision on the verdict wording: the flat-face node-curvature map
candidate IS recovered (all acceptance criteria pass). On the codec
side the tested grammar families are exhausted with **zero survivors**
— the "aliases" that remain are not multiple surviving new grammars
but the standing finite ambiguity between the DEMOTED old structural
profile (exact arithmetic, contradicted semantics) and the unresolved
true segmentation. No verdict string matches that situation exactly;
this is the closest, and this paragraph is the governing statement.

## Codec result

- The CYYT pair is decisive: under old boundaries, compact CYYT sits
  in the orange/Channel branch, 32.7° from St. John's — so
  `F5|Q22|S3` is demoted to `EXACT_OLD_STRUCTURAL_PROFILE` (its
  arithmetic and all prior receipts stand; its field SEMANTICS are no
  longer final authority).
- 183 declared candidates across families A–F: **0 survivors**. The
  scatter result (zero 3-bit-deletion supersets even per pair) proves
  refined wires are re-encodings, not appended-digit forms, at the
  whole-word level. Cross-phase: 261 candidates, 0 survivors.
- The typed-digit pattern 5→4 / 8→7 / 5→5 across the three pairs is
  preserved as data.
- Path forward is the source's explicit offer of purpose-built
  vectors (digit counts, faces, continents on request) — recorded in
  the private ledger with three concrete request suggestions.

## Map result (all acceptance criteria PASS)

`FLAT_FACE_NODE_CURVATURE_V1_CANDIDATE`: flat chord faces, affine
barycentric recursion, curvature only from lifted nodes (rho=1
spherical control profile), NO RBF, no displacement field. Fitted 24
node-direction parameters: anchors (Stonehenge/Erie/Toronto via the
demoted-but-exact old paths) to 4e-05°; Wilkes root 1e-6°; SAA phase
vertex unmoved; convex; 0 orientation reversals at depth 6 (81,920
facets); 0 shared-edge mismatches; exact hierarchical inverse
(60/60). Beats every control (midpoint 16.6°, Slerp 15.3°, Möbius
4.5°, Snyder-approx 16.9°) and matches V1's anchor exactness with 24
parameters instead of 627 stored warp steps. V1 is preserved and NOT
replaced — the new model's anchor semantics still ride on the demoted
profile's paths, so it is the source-profile CANDIDATE pending codec
resolution.

## Standing exclusions honored

Montréal direct record retained, excluded from fit (45.93° residual —
codec-layer tension, orientation-invariant per R10.10, unfixable by
any per-face smooth map while its path sits 0.257° from Stonehenge's
cell). UK cluster never fit, re-decoded post-freeze only, names
labelled model-derived. Gander/Argentia untouched. Sealed holdouts
never used for selection; new frozen predictions under the new map
(with V1 continuity values) are hashed; `1687209343` stays BLOCKED.

## Verdict basis / non-claims

Software results only. No source-origin validation, no physical
coordinate-system validation, no craft-physics validation (the 20:39
wording is provenance, nothing more). Publication HOLD; no tag, push,
release, or public-main merge.
