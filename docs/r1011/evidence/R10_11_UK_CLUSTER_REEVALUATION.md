# R10.11 British-Cluster Re-evaluation (Phase 11)

Authority unchanged from the pack audit: the eleven vectors are
source-reported "additional landing sites" on a reported
common-traversal route; the NAMED places are V1 software outputs
under a Stonehenge-calibrated map — model-derived, not independent
labels. **They were not used to fit the new map or select any codec
grammar (test-enforced), and none of the V1 names is treated as
confirmed.**

Post-freeze re-decode under `FLAT_FACE_NODE_CURVATURE_V1_CANDIDATE`
(`R10_11_UK_CLUSTER_NEWMAP.csv`): all eleven remain in southern/central
Britain but shift east-northeast by roughly 0.5–1.5° relative to their
V1 outputs (e.g. the vector V1 placed near Stafford moves to
~52.57 N, 1.05 W). The corridor structure survives qualitatively;
exact cross-track statistics belong to a later phase once the codec
is resolved, because every position here still rides on the DEMOTED
old structural paths.

Classification after this phase:

- vector values: source-reported;
- landing-site / common-route classifications: source-reported;
- named places and coordinates (V1 or new-map): MODEL-DERIVED,
  calibration-relative;
- independent geographic validation: none;
- use in codec or map fitting: none.
