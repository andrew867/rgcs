# RGCS v2 — System Architecture

> **Reading note.** This document is the **v2 system-level record** and is
> preserved unchanged. For the current five-lane package map across all
> release generations, see the [v8 addendum](#v8-addendum--the-wider-package-map)
> at the end, or the [documentation index](README.md).

**Author:** Sub-Agent 09. **Date:** 2026-07-14. Companion documents:
`CORE_API_SPEC.md` (library-level contract) and `DESKTOP_ARCHITECTURE.md`
(desktop internals). This document is the system-level view.

## 1. Layering (strict, one-way)

```
manuscript/  tools/make_*.py           (build-time consumers)
        │
rgcs_desktop/                          (PySide6 UI, workspaces, jobs)
        │  imports only public rgcs_core API
rgcs_core/                             (pure computation; no UI, no I/O
        │                               beyond provenance file hashing)
docs/model_registry.yaml               (equation contract RGCS-M.1..M.61)
```

Rules (QA-verified: no leakage in either direction):
- `rgcs_core` never imports Qt, never touches workspaces, never prints.
- `rgcs_desktop` performs no physics: every number displayed comes from a
  core call; viewers carry the core's classification metadata into badges.
- `tools/` and the manuscript consume the same public API as the desktop.

## 2. rgcs_core package map

| Package | Contents | Registry ids |
|---|---|---|
| `geometry` | polygon/cap/volume model, density inverse, spiral cone (path length, pitch, curvature, compact radius R_χ per M.36), node estimators, angle audits | M.1–M.12, M.30–M.39 |
| `harmonics` | 4096 ladder, axial half-wave, ladder lengths | M.13 group |
| `compact_modes` | compact-coordinate spectrum f_n² = f_b² + (nκ_χ)², parity families, zero-mode flag | M.13–M.17 |
| `resonance` | resonance offset ε_R^(f), class bins from Q, linewidths | M.18–M.22 |
| `coupled_modes` | 2-mode/N-mode eigenproblems, avoided crossings, coupling map \|K\| = 2πg (anti-Hermitian, QA-D-04), Stuart–Landau dynamics | M.23–M.29, M.46–M.49 |
| `coherence` | COH-M1..M14: analytic signal, circular stats, Rayleigh test, windowed autocorrelation coherence + baseline, PLV, onset/decay, anisotropy tensor, decay-law model comparison | M.51–M.56, M.61 |
| `loading` | added modal mass, loading factor k_H | M.40s |
| `drive` | pulse families, exact-cycle allocation, phase residue, micro-pulse metrics | M.57–M.60 |
| `experiments` | control-subtracted metrics, run-record serialization | — |
| `provenance` | classification decorator, sha256 helpers, canonical JSON, MODEL_VERSION | — |
| `uncertainty` | UncertainValue (mean, σ, interval propagation) | M.10 |

## 3. rgcs_desktop subsystems

- **app/** — entry point (guarded startup auto-open with backup restore,
  QA-D-05), `AppContext` (workspace + job manager + settings), main window
  (13 panels, sidebar, inspector, command palette).
- **workspaces/** — one directory per project; SQLite registry
  (objects/files/jobs/exports) + content-addressed artifacts; atomic
  writes; backup-on-open; integrity check before backup; `WorkspaceError`
  on corruption; `Workspace.restore_latest_backup()`.
- **jobs/** — process-based job manager (spawn), progress/log streaming,
  cancellation; workers for coherence analysis (CSV loader with
  user-facing errors and finiteness gate, QA-D-06/13).
- **services/** — schema validation (strict, versioned, friendly errors
  incl. malformed JSON, QA-D-07), claim gates (coherence claim gate,
  ethics gate), formatting.
- **viewers/, widgets/, plots/** — panels, classification badges,
  uncertainty bands.

## 4. Build/packaging paths

- Library: `pip install .` (pyproject; extras `desktop`, `dev`).
- Desktop binary: PyInstaller spec `tools/packaging/rgcs_desktop.spec`;
  Linux build script `tools/packaging/build_linux.sh` (artifact under
  `release/linux/`); Windows path documented in
  `tools/packaging/build_windows.md`.
- Manuscript: `python3 tools/make_figures.py && python3 tools/make_tables.py
  && latexmk -xelatex manuscript/rgcs_v2.tex`.

## 5. Key architectural decisions

1. **Registry-first equations** — code cannot ship an equation without a
   registry id; provenance decorators make labels machine-checkable.
2. **Complex canonical dynamics** — M.46 integrated in complex form
   (polar form singular at A→0); exponential integrating factor for the
   stiff linear part.
3. **Content-addressed artifacts** — deterministic ids (sha256 of
   canonical JSON) make identical results collide instead of duplicate.
4. **Process-isolated jobs** — analysis crashes become failed jobs with
   preserved tracebacks, never app crashes.
5. **Golden data as contract** — expected values live in
   `experiments/sample_data/golden_coherence/manifest.json`, read by tests
   and regenerated byte-identically by `tools/generate_golden_coherence.py`.

---

# v8 addendum — the wider package map

*Added 2026-07-30 (R10.62). Everything above describes the **v2 core and
desktop**, and is unchanged. It is still accurate for `rgcs_core` and
`rgcs_desktop`. What follows is the system-level view after six further
release generations, in which RGCS grew from one lane into five.*

## 1. The layering rule still holds, generalised

The one-way rule from §1 was never relaxed. It now reads:

```
applications        rgcs_desktop/  rgcs_lab/  rgcs_workbench/  r1053 CLI
        │           (UI, HTTP, CLI — no physics, no geometry)
        │
lanes               crystal · coordinates · discovery · speculative
        │           (each owns its domain; lanes never import each other)
        │
foundations         rgcs_core (v2)  ·  rscs_core (RSCS 1.0)
                    rscs2_core (RSCS 2.0 multiphysics)
        │
contracts           docs/model_registry.yaml       RGCS-M.1..M.61
                    rscs2_core/registry/*.yaml     RSCS2-*
```

Two invariants carried forward from §1, and both are still QA-verified:

- **No physics in an application.** Every number a UI displays comes from
  a lane or foundation call, carrying its classification metadata.
- **Frozen authorities are never edited.** New work adds registry ids; it
  does not modify v2/v3 ones. Tags `v2.0.0`/`v3.0.x` and
  `archive/v2.0.0/` remain byte-frozen.

One invariant is new:

- **Lanes do not import each other.** A finding in the coordinate lane
  cannot silently become an input to the crystal lane. Cross-lane use goes
  through a documented contract or not at all.

## 2. Lane map

### Foundations

| Package | Contents |
|---|---|
| `rgcs_core` | the v2 computational core mapped in §2 above — geometry, harmonics, compact_modes, resonance, coupled_modes, coherence, loading, drive, experiments, provenance, uncertainty |
| `rscs_core` | RSCS 1.0 — typed coordinate/state-space foundation: periodic phase ambiguity, nested frames, uncertainty propagation, provenance |
| `rscs2_core` | RSCS 2.0 — capability-aware multiphysics: anisotropic FEM, piezoelectric coupling, eye diagnostics, consensus engine, optional OpenCL backend |

### Crystal & resonator lane

| Package | Contents |
|---|---|
| `r1013` | custom crystal specimen workflow — schema, Christoffel API, FEM + convergence + piezo boundaries, unified CLI (`rgcs`) |
| `resonator_platform` | closed-loop platform: design → simulate → fabricate → fixture → excite → measure → identify modes |
| `fkey_instrument` | frequency-key harmonic excitation; ESP32-CYD instrument |
| `rgcs_surface_wave` | phase-gated dielectric-loaded annular surface-wave model |
| `r1015a` | Scale A mechanical crystal lane |
| `cspc`, `pmwr` | lore-derived, evidence-gated programmes; not load-bearing for any established result |

### Coordinates & mapping lane

| Package | Contents |
|---|---|
| `rgcs_coordinate` | typed structural decoder/encoder for the candidate 30-bit codec |
| `cwatlas` | CW Atlas and bidirectional geocoder |
| `r1053` | V1 Earth-root map workbench — kernel, projector, certificate, pathmap, polygon, screenshots, CLI |
| `r1016`–`r1034`, `r109` | the R10.x addressing research chain that produced it |

`r1053` follows the same internal discipline as `rgcs_core`: computation
is pure, artifacts are content-addressed, and every emitted coordinate
carries a frame manifest, a claim class, and its applicable blockers. It
never emits a bare latitude/longitude.

### Discovery & experiment architecture

| Package | Contents |
|---|---|
| `r13` | cross-domain discovery architecture — response functions, Green functions, S-matrices |
| `r15` | experimental phase infrastructure; makes the R13 architecture instrument-ready and calibration-bound |

### Quarantined

| Package | Contract |
|---|---|
| `consciousness_lane` | isolated under an explicit quarantine contract. No other package may import it, and it may not influence any adjudication. |

## 3. Applications

| Entry point | What it is |
|---|---|
| `rgcs-workbench` (`python -m rgcs_desktop`) | the PySide6 desktop workbench of §3 above |
| `rgcs-lab serve` | local FastAPI hub — nine inspectable modules, receipts, claim badges; loopback-only, telemetry off |
| `rgcs` | unified crystal workflow CLI (`r1013`) |
| `rgcs-v4` | RSCS 2.0 multiphysics CLI |
| `rgcs-coordinate` | coordinate codec CLI |
| `rgcs-workbook` | workbook / reporting CLI |
| `python -m r1053` | map workbench — `parse`, `map`, `path`, `polygon`, `serve` |

## 4. Architectural decisions added since v2

The five decisions in §5 all still hold. Four more have been added:

6. **A lane may be refuted without being deleted.** `cspc`, `pmwr` and the
   surface-wave lane exist so speculative material can be attacked. Their
   null results are published beside the positive ones rather than
   removing the code that produced them.
7. **Refusal is a return value.** Wide-envelope records are *refused*, not
   truncated; a self-crossing polygon reports no area; a criterion with no
   dataset reports `NOT_SCOREABLE` rather than guessing. An honest refusal
   is a first-class output.
8. **Fitted quantities are labelled at the point of emission.** Anything
   that agrees because it was constructed to agree carries
   `TRAINING_EQUALITY`, in the JSON and not merely in prose.
9. **A second implementation must be checked against the first.** The map
   workbench ports its projector to JavaScript for the browser; a test
   runs the real page script and fails on drift above one millimetre.

## 5. Where to read next

[Documentation index](README.md) · [V1 Coordinate System](V1_COORDINATE_SYSTEM.md)
· [Claim Boundaries](CLAIM_BOUNDARIES.md) · [Blockers B01–B07](BLOCKERS_B01_B07.md)
