# Layout QA Report — v3 Manuscripts (Agent 09)

**Date:** 2026-07-15. Builds: XeLaTeX (MiKTeX), fonts loaded by filename
(TeX Gyre Pagella/Heros, DejaVu Sans Mono). Every numeric table, macro
value, and figure generated by `tools/make_v3_artifacts.py`; packaging by
`tools/package_manuscripts.py` (CHECKSUMS.json, VERSIONS.json, BUILD.md
per manuscript).

| Manuscript | Pages | Undefined refs | Overfull boxes | Landscape use | Notes |
|---|---|---|---|---|---|
| rscs_foundations | 5 | 0 | 0 | no | TikZ architecture diagram; longtables for 17 coords / 23 ops; CEP battery table |
| rgcs_crystal_application | 3 | 0 | 0 | no | TikZ forward hierarchy; anisotropy sweep figure with v2 band |
| software_hardware_plan | 3 | 0 | 0 | no | macro-envelope timing figure; closure + modulation tables |
| historical_source_companion | 5 | 0 | 0 | **yes** (adaptation ledger longtable) | 19-row EP ledger with forbidden-transfer column |

Fixes applied during layout QA:
1. Coordinate-units column: `/`-separated composite unit strings given
   `\allowbreak` break points (was 15 pt overfull).
2. Modulation-family table: converted from `S`-column to plain `r l l r`
   (header wider than `S[table-format]`; was 4.5 pt overfull).
3. Fonts loaded by filename so the build does not depend on OS font
   registration (MiKTeX/Windows portability).
4. Classification tags made math-mode-safe (`\mbox`).

Remaining known limits: the manuscripts are deliberately concise spines
over the fuller repository documentation (each section cites its source
doc); page counts will grow at Agent 11 regeneration only if content is
added, never by hand-typed numbers.
