# R10.8.5 runlog

```
python tools/r1085_octal_packet_recovery.py
python -m pytest tests/cwatlas/r1085/ -q
```

Reuses r12.icosapacket / r12.icosarefine verbatim (no new operator). R10.8.4 decimal-triplet decoder demoted to REJECTED candidate in cwatlas/r1082/decoder_candidates.py; its code is retained as the rejected-experiment receipt.
