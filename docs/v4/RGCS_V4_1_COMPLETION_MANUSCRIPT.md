# RGCS v4.1 — Completion Manuscript (capability-aware coupled-field platform)

All results are computational; the historical eye is a Source claim;
computed candidates are diagnostics; no experimental confirmation
exists; CPU float64 remains the numerical authority; missing
capabilities are software statements, never physical-nonexistence
claims. Continuity: v2/v3 frozen; v4.0.0 released 2026-07-16 and
frozen; this wave (the v4-completion programme) ships as v4.1.0.

## 1. Provenance and capability foundations (M1/M2)

A 20-record source registry (only the locally-present pack is hashed;
absent sources are metadata-only with null hashes — DV4C-003), a
15-equation ledger with classification ceilings that mechanically
block laundering (SRC/HYP can never become EST/DER; the ceiling test
caught a real ledger mis-sourcing at birth), an exclusion matrix, a
lawful-release filter, and a provenance linter. Above it, the
capability firewall: 24 typed capabilities × 16 registered
materials/references, an applicability service (UNKNOWN ≠ permission;
unimplemented ≠ nonexistent), a 13-block coupling graph that rejects
unsupported edges before numerics and quarantines the
source_hypothesis block, and a deterministic result envelope where
NOT_APPLICABLE means null-plus-reason, never zero.

## 2. Typed quantity taxonomy (M3)

Twelve registered torsion/circulation/angular-momentum quantities with
forbidden aliases; physical identity between distinct quantities
raises by type. Validated: Saint-Venant torsion (exact + FEM 5%),
Frenet-Serret (helix exact, planar null, no fabricated values at
degenerate curvature), Stokes-checked circulation, canonical optical
SAM/OAM/vortex diagnostics (real-field winding guard; intrinsic vs
extrinsic OAM both demonstrated), chiral two-mode phonons with a
capability-gated Zeeman interface. The historical spacetime-torsion
entry has ceiling SOURCE_HYPOTHESIS and no solver.

## 3. Reduced-order reference systems (M4/M5/M7/M11/M12)

Exciton-magnon modulation (verified expansions and Jacobi-Anger
sidebands), avoided crossings anchored to the frozen v3 model at
1e-12, capability-gated block Hamiltonians, dressed-spin coherence
(deterministic, regime-honest), dynamic magnetoelectric tensors
(I/Q separated, KK-checked, sparse-spectrum refusal), metacrystal g2
transfer (declared bounded rule), LiNiPO4 IOME (direction-channel
writing with the IFE/thermal comparator triad and held-out saturation
comparison), nonlinear AFM trajectories (threshold, π slip, honest
multi-well overdrive), and phonon-controlled exchange with a
mechanism classifier that never mislabels parameter modulation as
hybridization. Every model refuses alpha quartz.

## 4. Quarantine (M13/M10)

FDT and source-lore content live in an import-firewalled namespace
(AST-verified across 20 default modules), ceilinged SOURCE_HYPOTHESIS,
with dimensional/algebraic/empirical audits (invertibility adds no
evidence), six pre-registered discriminators whose positive outcomes
can never claim exclusivity, and a translation registry where eight
motifs gained falsifiable analogues and the literal extraordinary
claims are honestly recorded as analogue-free.

## 5. Channels and the eye (M14/M9 + V4C-D-001)

Sixteen optical channels with mechanically-enforced declared
consumption and an automated ablation matrix separating IOME
(direction) from IFE (helicity), Cotton-Mouton/annealing
(polarization angle), photothermal (intensity), and vortex (charge)
responses. The Eye Consensus gained a typed vote layer (unsupported
mechanisms excluded from counting, their absence never counted against
an anomaly) and — after a user-identified P1 — an uncertainty-aware
node-coincidence rule: exact coincidence is numerical (1e-6 mm);
interval overlap is a statement about localization uncertainty; a
resolved separation is reported at its exact value.

**Canonical 110 mm result (corrected):** the stable 3-family region at
(−0.295, −0.205, 102.240) mm lies exactly 3.906 mm from the nearest
conventional node/antinode station; with mesh-dominated localization
(±3.08 mm/side) the intervals overlap →
UNCERTAINTY_OVERLAPS_CONVENTIONAL_NODE ("may explain within
uncertainty" — statement 2 of 4, never collapsed into "explains").
The candidate is distinct from the geometric centre and the frozen
RGCS prior by ~47–51 mm; the male junction plane (~0.4 mm) is a
viable, untested conventional explanation; clmax ≈ 4 mm meshes would
discriminate. Result preserved; nothing rounded away.

## 6. Calibration (M8)

Hash-chained immutable observations, deterministic fits with
identifiability flags, drift alerts without mutation, diverse inverse
design with honest infeasibility, a policy guard making scientific
classification immutable to optimizers, and an off-by-default
simulation-only RL adapter.

## 7. Records

Claim/risk/traceability registers: `docs/v4/registers/`. Defects:
V4C-D-001 (node threshold, user-identified, corrected with a
regression battery) plus the in-development catches recorded in the
run logs. Binding scope: `WHAT_THIS_QUARTZ_MODEL_DOES_NOT_INCLUDE.md`;
all-materials limits: `V4_WHAT_IS_NOT_MODELLED.md`.
