# V4 Implementation Reality Matrix (Agent B0)

What actually exists at baseline `2fed8fd` (module map with byte/def
counts and marker heuristics in V4_MODULE_STATUS.json). Statuses use
the programme's classes.

## Implemented and validated (v4.0.0, CORE_VALIDATED unless noted)

| module | reality |
|---|---|
| `rscs2_core/fem.py` | anisotropic/isotropic FEM, modal solve (bit-deterministic), Robin/surface-mass BCs, harmonic response — benchmark-anchored |
| `rscs2_core/quartz.py` | frozen-constant tensors, rotations, Christoffel — anchored to v3 |
| `rscs2_core/crystal110.py` | canonical geometry + deterministic gmsh meshing |
| `rscs2_core/piezo.py` | stress-charge saddle solver, energy-patch validated |
| `rscs2_core/accel.py` | OpenCL Christoffel; Iris Xe/i5 HARDWARE_BENCHMARKED; CUDA = INTERFACE_ONLY (honest markers detected by scanner) |
| `rscs2_core/projections.py` | optical/coil/drive projections; octave presets declared arithmetic |
| `rscs2_core/eye.py` | 16 diagnostics D1–D16 + consensus (STABLE requires persistence evidence) |
| `rscs2_core/refsystems.py` | cavity (exact) + tuning fork (weak-coupling pair, V.9) |
| `rscs2_core/proofbundle.py`, `cli.py` | one-command 111-file bundle; 14-command CLI |
| `tools/qa_audit_v4.py`, `demo_v4.py`, `build_v4_release.py` | QA audit 19/19; 10-step demo; release assets |

## Absent (required by this completion programme)

| required by | missing at baseline |
|---|---|
| M1 | `sources/registry/` v4 source registry, equation ledger, adaptation/exclusion matrices, provenance linter |
| M2 | MaterialCapabilities schema, material/reference records, coupling graph, applicability service, result envelope |
| M3 | torsion/AM quantity registry, Frenet-Serret, Saint-Venant benchmark, SAM/OAM diagnostics as typed channels, chiral two-mode phonon |
| M4 | exciton-magnon, avoided-crossing reference (beyond frozen coupled_two_mode), block Hamiltonian, dressed spin |
| M5 | dynamic magnetoelectric tensor model |
| M6 | boundary schedules, modal projection/mixing, work-energy ledger, symmetry-lowering engine |
| M7 | metacrystal g2 transfer model |
| M8 | calibration/inverse-design core |
| M11/M12 | LiNiPO4 IOME, nonlinear spin trajectory, phonon-controlled exchange |
| M13/M10 | FDT quarantine package, source-lore registry |
| M14 | optical channel taxonomy + ablation engine |
| M9 | eye vote expansion + WHAT_THIS_QUARTZ_MODEL_DOES_NOT_INCLUDE |
| D1/Q1/R1 | completion docs, adversarial QA v2, v4.1.0 release chain |

## Partial / adjacent

- `rscs2_core/eye.py` D9/D10/D12 evaluators exist but refuse to run
  without solved inputs (honest INTERFACE-style refusal) — M3/M14
  supply real optical fields.
- `PolarizationState` (RSCS-C.9, frozen) provides Jones/Stokes — M14
  builds channels on it, never replaces it.
- v3 `coupled_modes.static.coupled_two_mode` is the frozen two-mode
  anchor — M4's avoided-crossing reference must anchor to it.

## Mocks / placeholders / stale artifacts

Scanner heuristics (V4_MODULE_STATUS.json markers): no
NotImplementedError stubs in shipped rscs2_core paths other than the
declared CUDA interface raise; no unknown-producer generated assets
inside the proof bundle (all regenerated by `proofbundle.py` at build
time). Untracked `release/v4/` zips are generated release assets
(deliberately untracked per the v3 lesson) — not user work, excluded
from baseline concerns.
