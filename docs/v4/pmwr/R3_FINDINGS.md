# RGCS v4.7.x R3 — Root-Space Resolver and Combined Lanes: findings

**Status: SOFTWARE_VERIFIED, PHYSICAL_UNTESTED.** No apparatus, no
data, no detected residual, no transport. The R3 lanes are typed
mathematics with refusal semantics; the strongest claim any
certificate can emit is a **bounded relational root lock**.

## Gate Zero

v4.7.0 re-verified by object ID and fresh remote download (workbook
hash MATCH); ancestry clean; **`enforce_admins` on main is now ON** —
the operator closed the R2 governance gap, so the branch-protection
gate is fully technical at last. Branch `v4.7x-r3` cut from verified
`origin/main`; all 30 tags preserved.

## The R3 correction

A root is **not** "the first place where the phase residual is zero".
`wrap(φ_obs − φ_model) = 0` means `φ_obs − φ_model = 2πn` for unknown
integer n — at 4096 Hz there are **4 097 zero-residual candidates per
second** of search window, and "first" is an arbitrary member of the
set (`ROOT_ALIAS_UNRESOLVED`). A root is a typed, calibrated,
alias-resolved, gauge-declared, uncertainty-bounded relational
reference state. Six typed root classes; six forbidden collapses
(hierarchy root ≠ vacuum origin, phase zero ≠ absolute phase,
canonical representative ≠ unique ontology, reference network ≠
preferred frame, …) enforced as executable refusals.

- **Alias resolution:** two incommensurate carriers (4096/4375 Hz)
  thin the alias set to one candidate per beat period — the v4.7
  dual-lattice result promoted to root level.
- **Gauge orbits:** invariant transformations leave an equivalence
  class; selecting a member requires a *declared* rule, and the
  representative is a convention, never "the one true state".
- **Emission coordinates:** four reference worldlines localize an
  event GPS-style (linearized solve recovers a planted position to
  mm); with three worldlines the honest output is
  `ROOT_PARTIALLY_IDENTIFIED`. The frame is relational by
  construction.
- **Root-lock certificate:** evaluates alias resolution,
  identifiability (rank/conditioning/posterior width vs frozen
  limits), independent-channel agreement, and holdout; emits one of
  the failure statuses or `ROOT_LOCK_BOUNDED`. The statuses
  `ABSOLUTE_VACUUM_ROOT_UNSUPPORTED` and
  `NONLOCAL_REFERENCE_FRAME_UNSUPPORTED` are standing.

## The other lanes, briefly

- **Phase lens (P01):** a calibrated linear forward operator with
  computed observability, Tikhonov inversion (unregularized inversion
  refused), an adjoint that passes the dot-product test to 1e-12, and
  a declared discrepancy floor. L ≠ Lᵀ is ordinary anisotropy, not
  one-way physics.
- **Tetrahedral addressing (P02/P03):** exact 8^d hierarchy with a
  round-trip-tested base-8 codec; a K value means nothing without one
  of its five declared semantics; π^d(K)=0 is the codec's bookkeeping
  root and refuses the vacuum reading. A destination certificate
  requires frame, epoch, metric, ephemeris, uncertainty, and
  calibration — otherwise an address is bookkeeping.
- **Spin/torsion (P04):** seven categories that never merge (adding
  classical angular momentum to intrinsic spin density is a type
  error). Einstein–Cartan torsion from a *fully polarized* solid is
  ~6×10⁻²⁵ m⁻¹ — unmeasurable by construction, stated not rounded up.
- **Metric-source boundary (P05):** the inverse Einstein calculator
  prices the wish: a part-per-billion metric change in a one-metre
  region needs **6.7×10¹⁷ kg** (~10¹³× the densest solid could supply
  in that volume). `REFUSED_BY_ARITHMETIC`; sensing (v4.6) remains
  the only supported metric capability.
- **Optical spin (P09):** SAM and OAM never merge into one scalar;
  voxel dose is ceiling-checked; quartz spin-texture transfer stays
  `OPERATIONAL_HYPOTHESIS`; relaxation refuses infinite memory and
  T2 > 2T1.
- **HAL memory (P10):** synthetic-only records on tetrahedral
  addresses with hierarchy-walking retrieval and a consent audit —
  personal data is refused at the constructor, and a consented lane
  would need consent records, revocation, and erasure *before* any
  ingestion.
- **Nested atlas (P11):** an Earth tetrahedral grid is a
  `REPRESENTATION_ARTIFACT`. The null-rotation campaign compares a
  claimed orientation against seeded random rotations: random
  landmarks never beat the ensemble; planted vertex landmarks are
  detected (positive control). "Portal" has no supported rung.
  Cross-frame addresses must name an ordered frame chain
  (Earth-fixed → barycentric → galactic).

## Source-log preservation (A50)

The L'ou/The L's log and companion notes are preserved verbatim in
the pack (`source_claims/`), sha256-pinned:
`9441467d…` (log), `605715ca…` (ingest policy), `ee494e36…` (lens
note), `5c0e5181…` (root-space interpretation), `22c7d751…` (spin
note), `ba0f9a20…` (address note). Status `LORE`/`SOURCE_CLAIM`/
`HYPOTHESIS_SEED`; the attribution records the operator's requested
label and does not prove an external origin. Future L'ou messages
follow the append-only ingest policy; none may retroactively redefine
a failed result.

## Adversarial campaign

Attacks that fail by test: selling the first residual zero as the
root; merging SAM+OAM or classical+intrinsic spin; unregularized
inversion; strong-field targets in the weak-field calculator;
personal data in HAL; unordered frame chains; grid orientations that
don't beat the null ensemble; any detected-Phryll state (grep-enforced
in `r3/` as in `pmwr/`); absolute-root language outside refusal
context.

## What would change any of this

Calibrated bench evidence under preregistered protocols with
independent replication. None exists.
