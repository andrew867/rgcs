internal-docs/
_to_delete/
__pycache__/
*.pyc
build/
dist/
*.egg-info/
release/linux/
.pytest_cache/
.venv/
.hypothesis/

# LaTeX build intermediates (v3 manuscripts; PDFs and sources ARE committed)
manuscripts/**/*.aux
manuscripts/**/*.log
manuscripts/**/*.out
manuscripts/**/*.toc
manuscripts/**/*.bbl
manuscripts/**/*.blg
manuscripts/**/*.fls
manuscripts/**/*.fdb_latexmk
manuscripts/**/*.xdv
manuscript/*.aux
manuscript/*.log
manuscript/*.out
manuscript/*.toc
manuscript/*.fls
manuscript/*.fdb_latexmk
manuscript/*.xdv
manuscript/*.blg

# Release ZIPs live on the GitHub release (checksummed in SHA256SUMS.txt),
# not in git history — tracking them nested each source zip inside the
# next and compounded ~30-60 MB per release.
release/*.zip

# v4 generated workspaces (demo + CLI scratch; proof_bundle_110mm IS committed)
demo_out/
cli_work/
