# RGCS Recursive Infrastructure Lab — distribution artifacts

- `static-hub/` — open `index.html` or serve with any static host
- Local server: `pip install -e ".[workbench]" && rgcs-lab serve`
- `SHA256SUMS.txt` covers the static hub tree

See `docs/user/rgcs_lab/QUICKSTART.md` and `docs/program/coordination/MERGE_NOTES_cursor.md`.
