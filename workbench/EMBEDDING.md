# Embedding

The safest initial deployment is a standalone page.

If embedding in another site, use an iframe with a restrictive sandbox and preserve the page’s no-network behavior. Do not add analytics that capture coordinate inputs without explicit consent.
