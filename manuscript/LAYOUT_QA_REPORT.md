# RGCS v2 Manuscript — Layout QA Report

**Author:** Sub-Agent 07 (Complete RGCS v2 Manuscript)
**Date:** 2026-07-14
**Document:** `manuscript/rgcs_v2.tex` → `manuscript/rgcs_v2.pdf`

## Build pipeline (reproducible)

```
python3 tools/make_figures.py     # 11 vector-PDF figures + fig_values.json (from rgcs_core 2.0.0)
python3 tools/make_tables.py      # 8 .tex tables + generated_values.tex (69 inline macros)
cd manuscript && latexmk -xelatex rgcs_v2.tex
```

Engine: XeLaTeX (TeX Live 2022 / latexmk 4.76), fonts TeX Gyre Pagella /
Heros / DejaVu Sans Mono. Bibliography: natbib + bibtex (`unsrtnat`);
biblatex/biber is NOT installed in this environment — do not convert.

## Compile status

- **Errors:** 0 (clean `latexmk -xelatex` run to completion).
- **Undefined references/citations:** 0. **Multiply-defined labels:** 0.
- **Pages:** 28 (letter), including one landscape page (the forward-hierarchy
  figure, Fig. 11, p. 21).
- **Overfull hboxes:** 1 remaining, 5.2 pt, in `tables/tab_spiral_node.tex`
  (a table row first line) — sub-visible at print scale; classified cosmetic
  and accepted. All others (originally 20, worst 89 pt) were fixed by column
  re-widths, `\small` longtables, wrappable symbol lists, and
  `\emergencystretch`.
- **Warnings:** 3 hyperref "Token not allowed in a PDF string" (math in two
  section titles → bookmarks degrade gracefully via `\texorpdfstring`);
  standard float/longtable info messages only.

## Content inventory

- 18 numbered sections exactly matching the 20-item brief (Abstract
  unnumbered; Appendices A–D = notation, derivations, algorithms,
  source-claim ledger/traceability/negative results).
- 11 figures (all `manuscript/figures/*.pdf`, vector, generated by
  `tools/make_figures.py`): architecture; compact ladder with ±u_v bands;
  parity families; epsilon map with Q-derived class bands; avoided crossing;
  4D spiral projection + per-turn radii; phase randomization (golden case d);
  coherence vs amplitude (golden case c); anisotropy-decay model comparison
  (seeded synthetic, exponential ground truth); control matrix; forward
  hierarchy (landscape).
- 8 generated tables + roadmap/notation/traceability longtables in the tex.
- 30 bibliography entries; all cited; no missing citations.

## Known cosmetic issues (accepted)

1. The 5.2 pt overfull noted above.
2. In Fig. 1 (architecture) the orange calibration-loop return arrow crosses
   two module boxes by design; the annotation label sits between rows.
3. Rayleigh statistics for golden case (d) are computed with slightly
   different per-run phase estimators in Fig. 7 (Z_R = 0.10) and Table 8
   (Z_R = 1.26); both are consistent-with-uniform (p = 0.91 / 0.28) and each
   figure/table states its own value. Agent 08 may unify the estimator if
   desired (`fig_phase_randomization` vs `tab_coherence_golden`).
4. Hyperref bookmark warnings for the two math-bearing subsection titles.

## Verification hooks for Agent 08

- Every inline number is a `\gv...` macro from
  `manuscript/tables/generated_values.tex`; regenerating with the two tools
  and recompiling must be byte-stable except for build timestamps.
- Golden values in Table 7 must match SOURCE_EVIDENCE_LEDGER Part E.
- Forbidden-vocabulary grep on `rgcs_v2.tex`: "condensat"/"BEC" appear only
  in source-summary and exclusion-list contexts; "quantum shear" absent;
  "spatial phase anisotropy" used throughout.
- x_g = 78.3277 mm / x_m = 77.0264 mm / male-frame 75.7250 mm match
  MATHEMATICAL_MODEL RGCS-M.39 (node example uses corpus cap heights
  h_f = 17.415434, h_m = 14.812763 as documented in the table caption).
